############################################################
## PATH
############################################################

if [ -d ~/.dotfiles ] ; then
  PATH="~/.dotfiles:${PATH}"
fi

PATH=.:$PATH
PATH=$PATH:/usr/local/bin:/usr/local/sbin