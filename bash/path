############################################################
## PATH
############################################################

if [ -d ~/.bin ] ; then
  PATH="~/.bin:${PATH}"
fi

PATH=.:$PATH
PATH=$PATH:/usr/local/bin:/usr/local/sbin